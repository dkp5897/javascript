const colors=['red','blue','gree','yellow','black'];

let index=colors.indexOf('blue');
console.log(index);

index=colors.indexOf('yellow');
console.log(index);

index=colors.indexOf('white');
console.log(index);