// it is also similar to slice method but the difference is that it's second parameter is length of extracted part not an index

let fruet='banana, apple, mango';

let ans = fruet.substr(2,9);
console.log(ans);

ans= fruet.substr(-5); // print from backward
console.log(ans);